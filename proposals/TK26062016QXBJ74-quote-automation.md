# 報價單自動化生成｜需求釐清與報價提案

> 案件編號 **TK26062016QXBJ74**｜資料自動化／Python｜可遠端｜預算 $20,000
> 提案方：e-life-ai（service.e-life-ai.com）

---

## 您的原始需求

```
基本需求：
供應商報價單 (不同格式)
        ↓
系統辨識資料 (70% 常配合供應商可寫固定欄位判讀，30% 需串接 API)
        ↓
產生成本分析 (固定格式)
        ↓
產生客戶報價單 (固定格式)
```

> 以下提案即依此需求逐項對應。我們完整保留您的原始描述，方便您逐條對照我們的理解與做法。

---

## 一、我們對這個需求的理解

貴公司目前的作業流程是：收到**各家供應商的報價單（格式不一）**，由人工讀出料號、品名、規格、數量、單價、交期、幣別等欄位，自行計算成本（加成、運費、匯率、稅），再依固定格式整理出**成本分析表**與**對客戶的報價單**。

這條流程裡，真正花時間且容易出錯的有三段：

1. **讀供應商報價單**：每家格式都不一樣，欄位位置、單位寫法、幣別各異，人工逐張判讀既慢又易誤植。
2. **算成本**：加成比例、匯率、運費、稅務攤提，公式雖固定，但每張手算仍有出錯空間，且改一個參數要全部重算。
3. **出客戶報價單**：成本確定後，再依固定格式重新排版成對外報價，等於同一筆資料謄打兩次。

因此本案的核心是：**打造一套工具，把「供應商報價單進來 → 辨識資料 → 自動算成本 → 自動產出客戶報價單」這整條人工作業自動化，並在資料異常時主動標記。** 您在需求裡已點出關鍵分法：約 **70% 的欄位**因常配合固定供應商、格式穩定，可寫固定規則直接判讀；其餘 **30%** 需串接外部 API 取得。我們完全認同這個分層，並以此為基礎設計方案：穩定的多數用最精準零成本的固定規則處理，少數變動的才向外部 API 取值，**讓系統長期可用、且不增加校正人力**。本案的辨識、計算、輸出三段，本質上多是確定性的規則邏輯，我們不為了導入 AI 而導入；只有在報價單格式真的發散到規則難以窮舉、或遇上掃描影像時，才作為可選的輔助手段。

---

## 二、這套工具怎麼運作（一張圖看懂）

```
  供應商報價單進來      Excel / PDF / Word，格式不一
   (多家、多格式)
       │
       ▼
  ┌─ A 報價單本身 ── 約 70%：資料就在檔案裡，格式穩定，
  │  (檔案內可取)     直接讀取檔案欄位、或以固定規則對位，
  │                   取得料號、品名、數量、單價、幣別等，
  │                   精準且零成本，不需 OCR 或 AI
  │
  ├─ B 格式偏差 ──── 同在報價單內，但欄位錯位、合併儲存格、
  │  (報價單內,需校正) 單位寫法不同等，以針對性程式碼校正回
  │                   標準格式（掃描影像才需 OCR 讀出文字）
  │
  └─ C 串接 API ──── 約 30%：報價單上沒有、需向外部查得的
     (報價單外,需外取) 資料（料號規格、即時報價、匯率等），
                      向外部串接 API 取得
       │
       ▼
  自動成本分析       依固定公式計算：成本加成、運費、匯率換算、
   (固定格式)         稅務攤提 → 產出固定格式的成本分析表
       │
       ▼
  自動規則驗證       依固定規則核對辨識值與計算結果：
   (確定性檢查)       ✓ 公式核對  ✓ 交叉一致性
                      ✓ 必填/完整性  ✓ 極值/異常標記
       │
       ▼
  自動產生報價單     成本確定後，依固定格式自動排版成
   (固定格式)         對客戶的報價單，無需二次謄打
       │
       ▼
  ⚠ 僅異常項浮出     未通過規則檢查的「極少數」才標記出來
       │
       ▼
  人工複核           日常無需介入；僅就標記項確認後存檔即可
   (末端把關,非日常)  確認一次系統即記錄，同型報價單後續自動通過
```

核心原則為**分層處理、優先採用最穩定精準的方法**：常配合供應商、格式穩定的 70% 由 **A（固定規則，最穩定精準）** 直接判讀；已知特例的偏差交由 **B（特例程式碼）** 校正；需向外部取得的 30% 才以 **C（串接 API）** 補足。資料齊全後，**成本分析與客戶報價單兩段固定格式的產出，全程由系統依固定公式自動完成**，把同一筆資料只讀一次、算一次、出兩份文件，徹底省去重複謄打。系統並以固定規則自動核對辨識值與計算結果，僅未通過檢查的極少數項目需人工複核。

---

## 三、兩段固定輸出：成本分析與客戶報價單

本案與單純「抽取」案的差別在於：辨識資料只是第一步，真正的價值在後面**兩段固定格式的自動產出**。我們的做法如下：

| 您的需求 | 我們的具體做法 |
|----------|--------------|
| **產生成本分析（固定格式）** | 辨識出供應商單價、數量、幣別後，系統依您既有的成本公式自動計算：成本加成、運費攤提、匯率換算、稅務等，輸出固定格式的成本分析表。公式參數（加成率、匯率來源）集中可設定，調一處即全部重算，不必逐張手改。 |
| **產生客戶報價單（固定格式）** | 成本確定後，依您對外的固定報價格式自動排版產出，同一筆資料不再謄打第二次。可一鍵由成本分析帶出對客戶報價，並保留兩者對應關係以利日後核對。 |
| **資料異常主動標記** | 每次執行後自動產出處理報告：本批成功幾張、異常幾張、失敗哪幾張及原因（格式不符、欄位缺漏、API 取值失敗等）。異常不中斷整批作業，問題集中列示，一覽即明。 |

### 自動規則驗證（確定性檢查，不需 AI）

系統對每筆辨識值與計算結果自動執行四項規則檢查，這些都是明確可定義的規則，不依賴 AI 判斷：

1. **公式核對**：單價×數量是否等於金額、加成與匯率換算是否正確、成本與報價是否符合既定公式。
2. **交叉一致性**：同一張報價單內欄位相互對照，成本分析與客戶報價單金額是否能對得起來。
3. **必填／完整性**：應有欄位是否缺漏、辨識結果是否為空值或亂碼、API 是否成功取回。
4. **極值／異常標記**：數值落在合理範圍外（負數、極端值、匯率或日期超出範圍）者即予標記，送末端複核。

> 簡言之，本工具是**辨識 → 自動算成本 → 自動出報價單 → 僅異常項目需人工複核**的完整閉環，驗證靠的是明確規則而非 AI。對貴公司而言，是節省每張報價單的重複判讀與謄打，而非增添校正人力。

---

## 四、資料從哪裡來：A／B／C 三種來源（呼應您的 70%／30%）

這一段釐清「每個欄位的資料從哪裡取得」，直接對應您需求中「70% 固定欄位、30% 串接 API」的分法。A、B、C 是**三種資料來源**，依各欄位實際情況採用、亦可在同一張報價單上並用：

| 來源 | 這些資料在哪 | 怎麼取得 |
|------|--------------|----------|
| **A. 報價單本身（約 70%）** | 資料就在供應商報價單檔案裡，常配合的供應商格式穩定 | **直接讀取檔案欄位**（Excel／CSV 等結構化檔），或以**固定規則（如 regex）對位**取得料號、品名、數量、單價、幣別等 |
| **B. 報價單裡有、但格式偏差** | 同樣在報價單內，只是欄位錯位、合併儲存格、單位寫法不一，或為掃描影像 | 以**針對性程式碼**校正回標準格式 |
| **C. 報價單裡沒有、需向外部取得（約 30%）** | 報價單上沒有、得另外查的資料：料號規格、即時報價、匯率等 | 向外部**串接 API** 取得 |

> **C 的工作量與成本，取決於那 30% 的「API」具體指什麼。** 這是本案最需先釐清的一點：是向供應商或第三方平台**查即時報價／料號規格**？是**匯率 API**？還是要接貴公司自家的 **ERP／系統**？不同來源在串接難度、是否需付費或授權金鑰、回應穩定度上差異很大。我們會在第六節把這點列為正式報價前提，看過實際 API 文件後才能將工作量講準。

**搭配方式**：同一套工具中三種來源並存混用，報價單本身有的採 A、格式偏差的用 B 校正、報價單沒有的才以 C 向外部取得，彼此互不衝突。實際各佔比重，依貴公司供應商報價單的實際樣貌而定。

### AI／OCR 是可選方案，不是非用不可

A、B 講的是「資料從哪來」；至於「怎麼把它讀出來」，多數情況下**直接讀檔案欄位或固定規則就夠了，不需要 AI**。AI 與 OCR 是橫跨 A／B 的**可選辨識手段**，只在以下情形才動用，且皆**可配合一般／輕量 CPU 的電腦運行、無需獨立顯卡**，資料留存本機：

- **掃描影像才需 OCR**：若報價單是掃描或拍照影像，沒有可直接讀取的欄位，才以成熟的輕量繁中辨識引擎讀出文字。純 Excel／文字檔則用不到。
- **格式真正發散時才用小模型**：若報價單格式發散到固定規則難以窮舉，才以可在一般 CPU 運行的輕量模型補足長尾欄位。供應商若多為固定模板，這一項通常用不到。

> 我們的取向是**能用固定規則就不用 AI**：把確定性的工作交給確定性的方法，準確且零成本、結果可重現；只有掃描影像與真正發散的長尾才動用 AI／OCR 輔助。實際是否需要、以及引擎選型，將於評估報價單樣本後確定。

---

## 五、本案交付範圍

| # | 交付物 | 內容 |
|---|--------|------|
| 1 | **報價單讀取程式** | Python 工具：讀取多格式供應商報價單 → 取出料號、品名、數量、單價、幣別等欄位 |
| 2 | **A／B／C 取值引擎** | 直接讀檔／固定規則（70%）+ 偏差校正程式碼 + API 串接（30%），三者可混用，依供應商實況搭配 |
| 3 | **自動成本分析** | 依固定公式計算成本加成、運費、匯率、稅務，產出固定格式成本分析表，參數集中可設定 |
| 4 | **自動客戶報價單** | 成本確定後依固定格式自動產出對客戶報價單，與成本分析保留對應關係 |
| 5 | **自動規則驗證** | 對辨識值與計算結果自動執行公式核對／交叉一致性／必填完整性／極值標記，確定性檢查、不依賴 AI |
| 6 | **自動處理報告（debug）** | 每批次成功／可疑／失敗清單與原因，問題集中呈現 |
| 7 | **操作說明 + 一次線上教學** | 檔案匯入、可疑項複核、報告判讀、參數設定，使同仁得以直接上手 |
| 8 | **原始碼交付** | 完整原始碼與註解，日後可自行維護或續約升級 |

---

## 六、報價前需與貴公司確認的事項

以下事項直接影響工作量與辨識準確率，懇請先行確認，此亦為本類案件成敗之關鍵：

1. **能否提供部分去識別化的真實供應商報價單範例？**
   此為最關鍵之素材。經評估實際檔案，方能判斷格式固定程度、是否需 OCR、固定規則可涵蓋之範圍。範例越多、越能涵蓋各供應商寫法，上線正確率越高。

2. **那 30% 的「串接 API」具體指什麼？**
   這是本案報價最關鍵的變數。是向供應商或第三方查即時報價／料號規格？是匯率 API？或接貴公司自家 ERP／系統？是否已有 API 文件、是否需付費或授權金鑰？懇請提供 API 文件或說明，我們才能將這部分工作量講準。

3. **供應商報價單為 Excel／Word／純文字 PDF，抑或掃描影像？**
   結構化的 Excel 命中率最高；純文字 PDF／Word 可直接讀取；若為掃描或拍照件，則需 OCR，工作量有所不同。

4. **成本分析的計算公式為何？**
   加成比例、運費與稅務攤提方式、匯率取得來源（固定值、每日更新、或由 API 取得）。懇請提供現行的成本分析表範例（欄位與公式）。

5. **客戶報價單的固定格式為何？**
   懇請提供現行對客戶的報價單範本（Excel／Word／PDF），含欄位順序、版面、是否需公司抬頭與印章欄位等。

6. **運行於何種設備、是否需排程？**
   單機桌上型電腦即可，抑或需排程自動處理？設備之 CPU／記憶體規格（若需用到掃描件 OCR，會影響其可運行規模）。

---

## 七、報價與分期

> 以下為依需求說明之初步報價區間，**正式報價於上述事項確認、看過報價單範例與 API 文件後出具。**

| 階段 | 交付內容 | 占比 |
|------|----------|------|
| 第一期：樣本分析與辨識核心 | 評估報價單範例、定義欄位、完成 A（70% 固定規則）辨識與資料結構 | 35% |
| 第二期：成本分析 + 報價單產出 | 依固定公式產出成本分析表與客戶報價單兩段固定格式輸出、規則驗證 | 40% |
| 第三期：API 串接 + 交付教學 | 串接那 30% 的外部 API、補偏差校正、處理報告、操作文件、一次線上教學、原始碼交付 | 25% |

**預算對應**：於「多格式報價單辨識 + A／B／C 分層 + 自動成本分析 + 自動客戶報價單 + 規則驗證 + 自動報告」之範圍內，可於 **$20,000** 預算內完成。**實際範圍取決於格式一致性與那 30% API 的性質**：若供應商多為固定模板、API 為現成且文件齊全（如公開匯率／查價 API），預算尚屬充裕；若報價單多為掃描件、或 API 需較深的客製對接（如自家 ERP、需付費授權），則須較多調校，將於評估範例與 API 文件後說明何者落於預算內、何者建議分期辦理。

**明確排除（如需，另行報價）**：
- 外部 API 本身的付費授權或訂閱費用（如第三方查價平台的使用費）
- 供應商報價單來源系統的對接（如自動由 email／ERP 收單），可後續加做
- 成本分析與報價單以外的報表、儀表板、BI 分析
- 上線後長期維運與大幅格式變更的持續調校（可另議維運合約）

---

## 八、時程

**確認規格與流程後，付訂後 5～7 日內完工**（視 API 串接複雜度而定；若 API 為現成且文件齊全，可更快）。針對貴公司提供之範本，先將 70% 固定格式（A）的辨識、成本分析與報價單兩段輸出建置完成、交付可用版本，其後再串接那 30% 的 API 並依實際使用狀況微調。

> **範本與 API 文件越齊全、交付越快、正確率越高。** 辨識準確率直接取決於所評估之真實格式種類；API 串接時程則取決於對方文件品質。貴公司初期提供之報價單樣本越多、API 說明越清楚，工具上線首日即可達致越高之準確率與越短之開發時程。

---

## 九、為什麼是我們

- **這是我們專精的領域**：資料辨識、固定規則處理、外部 API 串接、報表自動產出、Python 自動化，皆為我們曾產出之商業成品，而非試做樣品；需要時亦具備 OCR 與本地模型的實作經驗。
- **A／B／C 分層的務實取向**：完全呼應您「70% 固定、30% API」的判斷，不為導入 AI 而導入。本案的辨識、計算、驗證、輸出多是確定性的規則邏輯，我們就用最精準零成本的方式做；需外部取得的才串 API，掃描件或真正發散的長尾才考慮輔助辨識。如何把成本花在刀口上，我們具實務經驗。
- **不只辨識，更把兩段固定輸出做穩**：成本分析與客戶報價單的自動產出，是本案真正省人力的所在。我們把公式參數集中可設定、兩份文件保留對應關係，讓您改一個加成率或匯率即全部重算，而非逐張手改。
- **全端一站式、原始碼交付**：不轉包，附完整原始碼與文件，貴公司日後可自行維護或續約升級，不致交付後即失聯。

---

> 聯絡：service.e-life-ai.com｜enor@e-life-ai.com｜LINE @936pvpoq｜本提案有效期 14 天
> 數位綠洲有限公司｜統一編號 60512091
